
<h3><%=@event.name%></h3>

<table>
  <tr>
    <th>Name</th>
    <th>Personal Best</th>
  </tr>
  <tr>
  <%@swimmers.each do |swimmer|%>
    <td><%=swimmer.name%></td>
    <td><%=Timing.find_by(swimmer_id: swimmer.id, event_id: @event.id).time_to_string%></td>
    <%# This won't work because an event has many swimmers, we need to create Timing class that belongs to and event and also belongs to a swimmer %>
  <%end%>
  </tr>

</table>
